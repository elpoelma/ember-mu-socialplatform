<Navbar @isAuthenticated={{this.session.isAuthenticated}} @nickname={{this.account.userAccount.nickname}}/>
<div class="uk-container">
    <div class="uk-margin-large-top">
        {{outlet}}
    </div>
</div>
